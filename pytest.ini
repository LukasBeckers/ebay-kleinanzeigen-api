[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    live: hits the real kleinanzeigen.de (network required, slow, can break if their HTML changes)
    slow: longer-running test
addopts = -q -ra --strict-markers
